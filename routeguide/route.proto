syntax = "proto3";

option go_package = "./routeguide";

package routeguide;

service TokenService {
    rpc SendRequest(Request) returns (Response) {}
}

message Request {
    int32 status = 1;
    int32 message = 2;
}

message Response {
    int32 status = 1;
    int32 message = 2;
}